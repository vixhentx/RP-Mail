<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:models="clr-namespace:RPMailUI.Models"
             xmlns:converter="clr-namespace:RPMailUI.Converter"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="RPMailUI.Controls.ErrorView"
             x:Name="ErrorRoot">
    <ScrollViewer HorizontalAlignment="Stretch" x:Name="ScrollView">
            <ItemsControl x:Name="ErrorsControl">
                <ItemsControl.DataTemplates>
                    <DataTemplate x:DataType="models:ErrorItemData">
                        <Border Classes="error-item"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Stretch">
                            <Border.Background>
                                <SolidColorBrush Color="{CompiledBinding BackgroundKey, Converter={x:Static converter:ResourceColorConverter.Instance}}"/>
                            </Border.Background>
                            <DockPanel>
                                <SelectableTextBlock Text="{CompiledBinding Message}" Height="{CompiledBinding #ErrorRoot.CellHeight}" FontSize="18" FontWeight="Bold"
                                           VerticalAlignment="Center" HorizontalAlignment="Stretch" Padding="5"
                                           TextWrapping="Wrap" TextAlignment="Center">
                                    <SelectableTextBlock.Foreground>
										<SolidColorBrush Color="{CompiledBinding ForegroundKey, Converter={x:Static converter:ResourceColorConverter.Instance}}"/>
                                    </SelectableTextBlock.Foreground>
                                </SelectableTextBlock>
                            </DockPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.DataTemplates>
            </ItemsControl>
        </ScrollViewer>
</UserControl>
